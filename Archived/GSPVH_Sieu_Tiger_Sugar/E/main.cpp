#include <bits/stdc++.h>
#define ll long long
#define ii pair<int,int>
#define fi first
#define se second

using namespace std;

const unsigned long long highmax = 1e19 + 10;
const int lowmax = 3e6 + 300;

int findsqrt(unsigned long long n){
    long long x = sqrt(n);
    for (int i=-3; i<=3; i++){
        if (x)
    }
}

int main(){
    freopen("divpow.INP","r",stdin);
    freopen("divpow.OUT","w",stdout);
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    return 0;
}
